package Tests;

import static org.junit.Assert.*;
import java.awt.Color;
import javax.swing.text.html.HTMLDocument.HTMLReader.IsindexAction;
import org.hamcrest.core.IsInstanceOf;
import org.junit.Test;
import org.junit.Before;
import org.junit.After;

public class Testing {

	
	/**
	 * Test to add to many Intro Hours to a Module.
	 */	
	@Test
	public void testJumpPlayer(){
		
		
//		ruleSet.makeArrayEmpty();
//		player.setCol(8);
//		player.setRow(8);
//		player2.setCol(8);
//		player2.setRow(10);
//		Piece[][] p = bm.getBoard();
//		p[player.getRow()][player.getCol()] = player.getPlayerPiece();
//		p[player2.getRow()][player2.getCol()] = player2.getPlayerPiece();
//		ruleSet.viewPotentialMove(player2, bm.getBoard());
//		assertEquals(6,ruleSet.getPositions().size());
//		ruleSet.makeArrayEmpty();
//		ruleSet.viewPotentialMove(player, bm.getBoard());
//		assertEquals(6,ruleSet.getPositions().size());
	

	}
	
}

